
# 請求書チェックツール 仕様書
**更新日: 2026/02/07**
**バージョン: 2.0.0**

## 1. 異常検知ロジック (Anomaly Detection)

### 1-1. 基本前提
*   **毎月取引判定**: 直近4ヶ月間で **3回以上** の取引実績がある部門・取引先の組み合わせ。

### 1-2. 判定区分・条件
以下の優先順位で判定を行います（上位優先）。

1.  **月ズレ？ (Gap Check)**
    *   **条件**: 取引日付が「基準月の翌月」であり、かつ「当月（基準月）の個数が0」である場合。
    *   **判定**: 「月ズレ？」
    *   **備考**: 金額の増減に関わらず、翌月日付の請求書が発見された場合はこちらが優先されます。

2.  **毎月あるのに今月ない (Missing Check)**
    *   **条件**: 「毎月取引判定」を満たし、かつ「当月のデータが存在しない（0件）」場合。
    *   **判定**: 「毎月あるのに今月ない」
    *   **出力**: 
        *   **現場チェック用シート**: 出力されます（確認用）。
        *   **Excel / 経理チェック用シート**: 出力されません（対象外）。

3.  **二重入力？ (Duplicate Check)**
    *   **条件**: 以下の条件をすべて満たす場合。
        1.  「毎月取引判定」を満たす。
        2.  前月の個数が **1件以下** である。
        3.  当月の個数が **2件以上** である。
        4.  **【厳格化】当月の支払金額がすべて同一である**（例: 50,000円の請求書が2枚ある等）。
    *   **判定**: 「二重入力？」

---

## 2. ステータス判定・更新ロジック (Status Update)

### 2-1. 自動承認ロジック
入力データ（CSV）のステータス値に応じて、スプレッドシート上の値を自動更新します。

| 入力データのステータス | 更新後のスプレッドシート値 | 備考 |
| :--- | :--- | :--- |
| **支払確定** | **承認済** | 強制上書き |
| **全額決裁** | **承認済** | 強制上書き |
| **全額決済** | **承認済** | 強制上書き（表記揺れ対応） |
| **締未済** | **承認済** | 強制上書き |
| **未承認** | **(維持) または 未承認** | 既存値が「未承認」以外なら維持<br>そうでなければ「未承認」更新 |
| その他 | **未承認** | |

### 2-2. 手動入力値の保護
*   スプレッドシート上で手動変更されたステータス（「確認中」「保留」など）は、入力データが「未承認」や「その他」のままである限り、次回更新時に上書きされずに **維持** されます。
*   ただし、入力データが「支払確定」などに変わった場合は「承認済」に強制変更されます。

---

## 3. 出力仕様 (Output Specifications)

### 3-1. 経理チェック用スプレッドシート
*   **カラム構成**: 
    *   基本情報（部門、取引先、金額など） + OCR判定結果 + リンク + ...
    *   **末尾（右端）**: 「状況区分」カラムを追加（入力データのステータス値を表示）。
*   **条件付き書式**:
    *   ステータス（A列）が **「承認済」** の場合 ⇒ **行全体をグレーアウト**（背景色: 灰色）。

### 3-2. 現場チェック用（事業所チェック用）スプレッドシート
*   **対象データ**: 
    *   異常検知（二重入力？、月ズレ？、毎月あるのに今月ない）が判定されたデータのみ抽出。
    *   例外部門・除外取引先は出力しない。
*   **条件付き書式（色付け）**:
    *   **二重入力？** ⇒ **「取引先名」セル** を **赤色（薄い赤）** に変更。
    *   **月ズレ？** ⇒ **「取引日付」セル** を **黄色（薄い黄）** に変更。
    *   **毎月あるのに今月ない** ⇒ **「取引先名」セル** を **青色（薄い青）** に変更。

### 3-3. Excel出力 (OUTPUT file)
*   **出力内容**: 全データ（異常検知「毎月あるのに今月ない」を除く）。
*   **カラム構成**: 
    *   基本情報 + 3ヶ月前/2ヶ月前/1ヶ月前/当月/翌月の金額・個数推移。
    *   **末尾（右端）**: 「状況区分」カラム（入力データのステータス値を表示）。
*   **書式**:
    *   コード列（部門コード、取引先コード等）はすべて **文字列（Text）** として出力（0落ち防止）。

---

## 4. その他・システム仕様

### 4-1. ゼロ埋め（0落ち）防止
*   **Excel**: 全てのコード列（部門、取引先、科目など）のセル書式を「文字列」に設定。
*   **スプレッドシート**: 出力時にコードの先頭にシングルクォート `'` を付与し、強制的に文字列として扱う（`'00123456`）。
*   **照合ロジック**: 既存データとのマッチング時も、コードを8桁ゼロ埋めしてから比較を行う。

### 4-2. OCR突合仕様
*   **突合キー**: 「部門コード」＋「取引先コード」
*   **判定基準**: 
    *   候補が1件のみ ⇒ 自動結合。
    *   候補が複数 ⇒ **支払金額が最も近いもの** を採用（完全一致優先）。
