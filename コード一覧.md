# コード一覧（ファイル別リファレンス）

作成日: 2026-08-28
このファイルは、プロジェクト内の主要なソースコードファイルを一覧化し、それぞれの役割を後から見返せるようにまとめたものです。全体像は [CLAUDE.md](CLAUDE.md)、異常検知ロジック等の詳細仕様は [system_specification.md](system_specification.md) を参照してください。

---

## 1. app/ — FastAPIアプリケーション層（APIエンドポイント）

| ファイル | 役割 |
|---|---|
| `app/main.py` | FastAPIエントリポイント。ロガー設定、静的ファイル/テンプレートのマウント、各routerの登録、起動時のDB初期化とPDF_ARCHIVE内ZIPの自動展開、トップページ・ヘルスチェックを提供 |
| `app/routers/check.py` | 支払依頼書チェックのメインAPI（`/api/check`）。CSVアップロード→チェック処理実行→結果Excel出力・ステータス確認・月次更新（累積データ反映） |
| `app/routers/master.py` | 各種マスターデータ管理API（`/api/master`）。取引先・部門・科目・除外取引先・許容支払先・祝日・担当割当ルール・例外部門・AI設定・メモ等のCRUDとCSV取込 |
| `app/routers/rule.py` | 正マスター（税区分ルール・科目ルール・強制修正オーバーライド）管理API（`/api/rules`）。登録・削除・検索・統計取得 |
| `app/routers/assignment.py` | 担当割当管理API（`/api/assignment`）。入力CSVから部門・取引先を抽出して担当割当マスタを同期、担当者設定・コピー・スプレッドシート反映 |
| `app/routers/exclude.py` | 除外取引先管理API（`/api/exclude`）。チェック対象外取引先の一覧取得・追加（upsert）・削除 |
| `app/routers/sync.py` | Googleスプレッドシート同期API（`/api/sync`）。チェック結果をスプレッドシートへ書き込み、必要に応じてDriveへアップロード |
| `app/routers/settings.py` | アプリ設定管理API（`/api/settings`）。AIモデル設定（config.yaml書き換え）、Google認証情報アップロード、各種キー・バリュー設定、Driveキャッシュクリア |
| `app/routers/ocr.py` | 請求書OCR処理API（`/api/ocr`）。ZIP（承認番号フォルダ群）展開→OCR解析・突合実行、進捗確認、結果一覧・Excelエクスポート、元ファイルダウンロード |
| `app/routers/reconcile.py` | 取引先請求書突合API（`/api/reconcile`）。取引先ごとのテンプレート設定、シノニム・除外部門管理、突合実行、結果Excel生成・DL、スプレッドシート同期 |

---

## 2. domain/ — ドメイン層（ビジネスロジック）

### 2-1. domain/services/

| ファイル | 役割 |
|---|---|
| `check_service.py` | チェック処理全体を統括する `CheckService`。CSV取込→各種判定（支払先・税区分・科目・支払日・異常検知）→総合判定→DB保存→Excel出力の中核サービス |
| `cumulative_service.py` | `CumulativeService`。チェック実行結果のOKデータを累積テーブルへ反映し、14ヶ月より古いデータを削除する月次更新処理 |
| `result_saver.py` | チェック結果（サマリ/明細）をDBの `output_summary`/`output_detail` へ保存するロジック。OCR金額と支払金額の一致判定も担当 |
| `excel_exporter.py` | `ExcelExporter`。OCR結果などをopenpyxlで整形しExcel（BytesIO）としてエクスポート |
| `invoice_match_service.py` | `InvoiceMatchService`。ZIP展開後フォルダをスキャンし、OCR実行（`invoice_ocr`モジュール群利用）と請求書データの突合・スコアリングを行う非同期サービス |

### 2-2. domain/validators/

| ファイル | 役割 |
|---|---|
| `vendor_check.py` | 支払先相違チェック。取引先コードと支払先コードの一致（または許容支払先マスター登録有無）を判定 |
| `tax_category_check.py` | 税区分チェック。累積データの「正」の税区分と申請内容を比較 |
| `account_check.py` | 科目チェック。累積データの「正」の科目コードと申請内容を比較 |
| `payment_date_check.py` | 支払予定日チェック。取引先マスターの支払パターンから期待支払日を計算（休日判定含む）し、申請日と照合 |
| `anomaly_check.py` | ズレ・モレ・二重計上の異常検知。直近4ヶ月の件数・金額から「毎月取引」か判定し、異常パターンを検出 |
| `overall_check.py` | 各判定結果（OK/NG/-）から総合判定を算出（NGが1つでもあればNG、次に-があれば-、それ以外OK） |

---

## 3. features/vendor_invoice_reconcile/ — 請求一覧突合機能（新機能）

| ファイル | 役割 |
|---|---|
| `models.py` | `InvoiceRecord`（請求一覧の抽出行）、`ReconcileResult`（突合結果）、`RunInfo`（実行情報）、`TemplateConfig`（取引先別抽出設定・部門名マッピング・除外部門）等のPydanticモデル |
| `repositories/master_repository.py` | 既存SQLiteDBから取引先マスタ・事業所マスタ・E2支払実績・突合対象取引先・除外取引先を取得する `MasterRepository` |
| `repositories/settings_repository.py` | 取引先ごとの抽出設定（`TemplateConfig`）をJSON（`config/invoice_reconcile_settings.json`）で読み書きする `SettingsRepository` |
| `services/extractor.py` | 請求一覧ファイル（Excel/PDF）から行データを抽出する `ExcelExtractor`/`PDFExtractor` と、ファイル種別に応じ抽出器を返す `ExtractorFactory` |
| `services/matcher.py` | 請求一覧の生の事業所名を、シノニム辞書やマスタの事業所名と照合しE2事業所コードに紐付ける `DepartmentMatcher` |
| `services/reconciler.py` | 請求一覧とE2支払実績（当月・前後月）を突合し、OK/MISSING/EXCESS/DOUBLE_INPUT/DATE_DIFF/DATE_GAP/RECURRING_MISSING/UNMAPPED等を判定する `Reconciler`（本機能の中核） |
| `utils/excel_generator.py` | 突合結果とRunInfoから、Reconciliation/Unmapped/RunInfoの3シート構成の色分け・書式付きExcelレポートを生成する `ExcelGenerator` |
| `ui/__init__.py` | パッケージマーカーのみ（UI用ファイルは未実装、フロントは `ui/static/reconcile.js` 側） |

---

## 4. infra/ — インフラ層（DB・外部連携・出力）

| ファイル | 役割 |
|---|---|
| `database.py` | SQLite（WALモード）接続管理。DBパス・認証情報パスの解決、`schema.sql`によるDB初期化・リセット |
| `csv_loader.py` | E2出力CSV（明細・取引先/部門/科目/税区分マスタ等）を読み込み、部門コード正規化・伝票番号分解・日付変換等を行いDB格納形式に変換 |
| `rule_repository.py` | 税区分・科目の「正マスター」ルールおよび強制修正（override）をDBに対し検索・登録・削除する `RuleRepository` |
| `settings_repository.py` | `app_settings`テーブルへのキー・バリュー形式アプリ設定の取得・保存を行う `SettingsRepository` |
| `excel_writer.py` | openpyxlでチェック結果（OK/NG判定等）を色分け・書式付きExcelとして出力 |
| `spreadsheet_service.py` | gspreadでGoogleスプレッドシートへチェック結果を同期するメインサービス `SpreadsheetService`（DB取得・Drive連携アップロード・書式/入力規則適用） |
| `spreadsheet_service_ext.py` | `SpreadsheetService`拡張。現場用（突合用）シートへの同期（もれ・ズレ反映、古い行削除、書式/条件付き書式）を行う `SpreadsheetServiceExt` |
| `drive_service.py` | Google Drive APIラッパー（サービスアカウント認証）。共有ドライブ対応のフォルダ検索・作成・確認、ファイルアップロード |
| `holiday_api.py` | 内閣府公開の祝日CSV（Shift-JIS）を取得し `(日付, 祝日名)` リストに変換する非同期関数 |
| `retry_utils.py` | 通信エラー（requests/gspread由来含む）を検知し関数呼び出しを自動リトライする `call_with_retry` |
| `schema.sql` | メインDBスキーマ定義（下表参照） |
| `schema_part.sql` | 追加スキーマ。`vendor_reconciliation_target`（毎月請求があるはずの取引先＝突合対象マスタ）を定義 |

### `infra/schema.sql` の主要テーブル

| テーブル名 | 用途 |
|---|---|
| `cumulative` | 月次累積の支払データ（14ヶ月ローテーション） |
| `work_items` | WorkID（案件）ごとの担当・照合状態管理 |
| `fingerprints` | WorkIDマッチング用の明細ハッシュ |
| `masters_vendor` | 取引先マスタ（支払条件・銀行口座情報等） |
| `masters_allowed_payee` | 取引先ごとの許容支払先マスタ |
| `masters_exclude` | チェック対象から除外する取引先マスタ |
| `masters_assign_dept_rule` / `_override` / `masters_assign_vendor` | 担当割当ルール（部門範囲・個別例外・取引先別） |
| `masters_exception_dept` | 出力対象外の例外部門マスタ |
| `holidays` | 祝日マスタ |
| `masters_account` | 科目マスタ |
| `run_log` | バッチ実行ログ |
| `invoice_ocr_results` | 請求書OCR抽出結果と突合結果 |
| `output_summary` | チェック結果サマリ（判定結果・担当・過去実績等） |
| `output_detail` | 入力明細の保持（伝票単位） |
| `rule_tax_account_master` / `_audit` | 税区分・科目の正マスターと変更履歴（旧式） |
| `masters_department` | 部門タイプ（COST/SGA等）判定用マスタ |
| `rule_tax_master` / `rule_tax_rules` | 取引先単位・部門/タイプ別例外の税区分ルール |
| `rule_account_master` | 取引先×適用範囲別の科目ルール |
| `override_rule` | 個別の強制修正ルール |
| `app_settings` | アプリ設定の永続化 |

---

## 5. invoice_ocr/ — 請求書OCRモジュール

| ファイル | 役割 |
|---|---|
| `ai_ocr.py` | Gemini APIを使った画像OCR。DB保存のAPIキー取得やGemini経由での請求書画像OCR（蛍光ペンハイライト箇所を優先抽出） |
| `ai_ocr_snippet.py` | Gemini APIで請求書PDFから「当月請求額」「請求日」「請求元会社名」をJSON形式で抽出する専用関数 |
| `extractor.py` | OCRテキストから金額・インボイス番号・軽減税率・稟議書有無・請求書判定等を抽出（テキスト正規化・設定読込含む） |
| `folder_scanner.py` | ZIP展開後のフォルダ階層（部門/取引先/ステータス/承認番号）を解析し、承認番号単位で対象ファイルを収集 |
| `ocr_engine.py` | pyocr(Tesseract)ラッパー。依存関係チェック・読取精度スコア算出 |
| `pdf_tools.py` | PDF処理ユーティリティ。テキスト層抽出（文字PDF対応）・画像変換（OCR用）、pdfplumber/pdf2image/poppler依存チェック |
| `preprocess.py` | OCR前処理（回転補正・傾き補正・画像強調）。OpenCV等の画像処理のみで生成AI不使用 |
| `scoring.py` | OCR結果（金額・インボイス番号・軽減税率・テキスト品質等）から総合スコアを算出し、AI OCRへの切替要否を判定 |

---

## 6. scripts/ — 運用・保守スクリプト

### マイグレーション系（`migrate_*.py`） — DBスキーマの一回限りの更新スクリプト

| ファイル | 内容 |
|---|---|
| `migrate_account_master.py` | `masters_account`（科目マスタ）テーブルを新規作成 |
| `migrate_account_rules.py` | `rule_account_master`テーブルを作り直し |
| `migrate_add_account_expected_name.py` | `output_summary`に`account_expected_name`カラム追加 |
| `migrate_add_drive_columns.py` | `output_summary`にDrive連携用カラム追加 |
| `migrate_add_gemini_flag.py` | `vendors`に`gemini_flag`カラム追加 |
| `migrate_add_has_ringi.py` | `invoice_ocr_results`に`has_ringi`（稟議有無）カラム追加 |
| `migrate_add_names.py` | `invoice_ocr_results`に`dept_name`/`vendor_name`カラム追加 |
| `migrate_add_ocr_columns.py` | `output_summary`に`ocr_amount`/`ocr_file_link`カラム追加 |
| `migrate_add_ocr_match_status.py` | `output_summary`に`ocr_match_status`カラム追加 |
| `migrate_assignment_tables.py` | 担当割当関連テーブルに`dept_name`カラム等追加 |
| `migrate_invoice_ocr.py` | `invoice_ocr_results`テーブルを新規作成（初期スキーマ） |
| `migrate_invoice_ocr_v2.py` | `invoice_ocr_results`に`dept_code`/`vendor_code`カラム追加 |
| `migrate_invoice_ocr_v3.py` | `invoice_ocr_results`に`status`/`detected_date`カラム追加 |
| `migrate_tax_rules.py` | `rule_tax_rules`テーブルを作成 |
| `migrate_v3.py` | `vendor_reconciliation_target`テーブルを新規作成 |
| `migrate_vendor_notes.py` | `masters_note_labels`/`masters_vendor_notes`テーブルを新規作成 |

### デバッグ系（`debug_*.py`） — 使い捨ての状態確認スクリプト

| ファイル | 内容 |
|---|---|
| `debug_db_check.py` | `output_summary`に`ocr_match_status`列が存在するか検証 |
| `debug_db_structure.py` | DB内の全テーブル一覧・レコード数・ファイルサイズを表示 |
| `debug_invoice_service_run.py` | `InvoiceMatchService`を実DBに対して単体実行 |
| `debug_scan.py` | `invoice_ocr/ZIP_FILE_OUT`配下を`scan_folder`でスキャンし件数確認 |

### その他（DBセットアップ・データ投入・運用ツール）

| ファイル | 内容 |
|---|---|
| `apply_schema.py` | `infra/schema.sql`を読み込みDBに初期スキーマを適用 |
| `auto_register_depts.py` | `output_summary`にあるが`masters_department`未登録の部門コードを自動登録 |
| `check_ocr_status.py` | `run_log`の直近5件の実行状況（OCR行数含む）を表示 |
| `cleanup_duplicates.py` | `cumulative`の重複レコードをID最大のもののみ残して削除 |
| `fix_kamitabashi_settings.py` | 特定取引先のテンプレート設定に部門コードのシノニムを修正登録 |
| `insert_test_summary.py` | `run_log`/`output_summary`にテスト用の擬似実行データを挿入 |
| `inspect_ocr_folders.py` | `ZIP_FILE_OUT`配下のファイル数・PDF数・階層構造を集計表示 |
| `populate_target_vendors.py` | 過去4ヶ月中3ヶ月以上支払実績のある取引先を`vendor_reconciliation_target`に登録 |
| `run_invoice_matching.py` | 最新（またはTEST）の`run_id`で`InvoiceMatchService`の突合処理を実行 |
| `seed_departments.py` | `masters_department`に初期データ（管理部・製造部等）を投入 |
| `seed_rules_from_history.py` | 過去実績を元に勘定科目/税区分ルールを自動生成 |
| `test_ocr_result.py` | OCR結果Pydanticモデル（`OCRResultItem`）のレスポンス形状を簡易確認 |

---

## 7. ui/ — フロントエンド

| ファイル | 役割 |
|---|---|
| `ui/templates/index.html` | アプリ全体のHTMLテンプレート（Jinja2）。ヘッダー・タブナビゲーション（チェック実行／OCR突合／請求一覧突合／正マスター／強制修正／担当割当／設定）と各タブのマークアップ（約1290行） |
| `ui/static/app.js` | メイン画面制御用JS（約3090行）。チェック実行・OCR突合・正マスター管理・担当割当・設定タブ等のDOM操作・API呼び出し |
| `ui/static/reconcile.js` | 「請求一覧突合 (New)」タブ専用JS（約1178行）。取引先/部門読込、突合実行、シート同期、シノニム管理、突合対象取引先・除外部門管理 |
| `ui_mock/ui_mockindex.html` | 本実装前のUIモック（ダークテーマの見た目確認用、API連携なし） |

---

## 8. tests/ — テスト

| ファイル | 役割 |
|---|---|
| `tests/conftest.py` | pytest共通フィクスチャ。`holidays_set`（祝日セット）、`in_memory_db`（インメモリSQLite）等を提供 |
| `tests/unit/`, `tests/integration/` | テストディレクトリ（現状、実テストファイルは未作成） |

---

## 全体構成イメージ

```
app/          … FastAPI ルーター（API層）
domain/       … チェックロジック・バリデータ（ビジネスロジック層）
features/     … 請求一覧突合機能（新機能、独立モジュール）
infra/        … DB・Google連携・Excel出力（インフラ層）
invoice_ocr/  … OCR処理一式
scripts/      … DBマイグレーション・保守用スクリプト
ui/           … フロントエンド（HTML/JS）
tests/        … テスト（未整備）
```

## 更新方法

コード構成に大きな変更（ファイル追加・役割変更）があった場合は、このファイルも合わせて更新してください。
